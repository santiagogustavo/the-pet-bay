import React from 'react';
import { Container, LogoText } from './styles';

const Navbar = () => (
  <Container>
    <LogoText to="/">
      the pet bay
    </LogoText>
  </Container>
);

export default Navbar;
